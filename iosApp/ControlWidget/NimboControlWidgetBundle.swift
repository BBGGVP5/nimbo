import SwiftUI
import WidgetKit

/// Точка входа расширения.
///
/// Пункт управления держит элементы приложений начиная с iOS 18, и
/// минимальная система расширения поднята до неё же. Прятать элемент за
/// проверкой доступности внутри связки нельзя: система перечисляет элементы
/// при сборе метаданных, и спрятанный так элемент в галерею не попадает.
@main
struct NimboControlWidgetBundle: WidgetBundle {
    var body: some Widget {
        NimboControlWidget()
    }
}
